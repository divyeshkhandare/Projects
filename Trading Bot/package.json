{
  "name": "trading-bot",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run index.ts --watch",
    "client": "cd client && npm run dev",
    "client:build": "cd client && npm run build",
    "backtest": "bun run scripts/runBacktest.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
